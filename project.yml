name: WorkoutTracker
options:
  minimumXcodeGenVersion: 2.44.1
  xcodeVersion: "26.4"
settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: com.cam.workouttracker
    CURRENT_PROJECT_VERSION: 1
    MARKETING_VERSION: 1.0
    SWIFT_VERSION: 6.2
    TARGETED_DEVICE_FAMILY: 1
    IPHONEOS_DEPLOYMENT_TARGET: 26.2

configs:
  Debug: debug
  Release: release

targets:
  WorkoutTracker:
    type: application
    platform: iOS
    deploymentTarget: "26.2"
    sources:
      - path: WorkoutTracker
        excludes:
          - Info.plist
          - Resources
      - path: WorkoutTracker/Resources
        buildPhase: resources
    dependencies:
      - target: WorkoutTrackerWidgets
    settings:
      base:
        DEVELOPMENT_TEAM: W9YS23J6FT
        PRODUCT_NAME: WorkoutTracker
        INFOPLIST_FILE: WorkoutTracker/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor

  WorkoutTrackerWidgets:
    type: app-extension
    platform: iOS
    deploymentTarget: "26.2"
    sources:
      - path: WorkoutTrackerWidgets
        excludes:
          - Info.plist
      - path: WorkoutTracker/LiveActivities/WorkoutRestLiveActivityAttributes.swift
    settings:
      base:
        DEVELOPMENT_TEAM: W9YS23J6FT
        PRODUCT_NAME: WorkoutTrackerWidgets
        PRODUCT_BUNDLE_IDENTIFIER: com.cam.workouttracker.widgets
        INFOPLIST_FILE: WorkoutTrackerWidgets/Info.plist
        GENERATE_INFOPLIST_FILE: NO
        APPLICATION_EXTENSION_API_ONLY: YES

  WorkoutTrackerTests:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "26.2"
    sources:
      - path: WorkoutTrackerTests
    dependencies:
      - target: WorkoutTracker
    settings:
      base:
        PRODUCT_NAME: WorkoutTrackerTests
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/WorkoutTracker.app/WorkoutTracker"
        BUNDLE_LOADER: "$(TEST_HOST)"

  WorkoutTrackerBenchmarks:
    type: bundle.unit-test
    platform: iOS
    deploymentTarget: "26.2"
    sources:
      - path: WorkoutTrackerBenchmarks
    dependencies:
      - target: WorkoutTracker
    settings:
      base:
        PRODUCT_NAME: WorkoutTrackerBenchmarks
        GENERATE_INFOPLIST_FILE: YES
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/WorkoutTracker.app/WorkoutTracker"
        BUNDLE_LOADER: "$(TEST_HOST)"

  WorkoutTrackerUITests:
    type: bundle.ui-testing
    platform: iOS
    deploymentTarget: "26.2"
    sources:
      - path: WorkoutTrackerUITests
    dependencies:
      - target: WorkoutTracker
    settings:
      base:
        PRODUCT_NAME: WorkoutTrackerUITests
        GENERATE_INFOPLIST_FILE: YES
        TEST_TARGET_NAME: WorkoutTracker

schemes:
  WorkoutTracker:
    build:
      targets:
        WorkoutTracker: all
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      targets:
        - name: WorkoutTrackerTests
        - name: WorkoutTrackerUITests
    archive:
      config: Release
  WorkoutTrackerBenchmarks:
    build:
      targets:
        WorkoutTracker: all
    test:
      config: Debug
      gatherCoverageData: false
      targets:
        - name: WorkoutTrackerBenchmarks
